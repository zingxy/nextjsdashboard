import { Inter, Fira_Code, Lusitana } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
});

export const firaCode = Fira_Code();

export const lusitana = Lusitana({
  weight: ['400', '700'],
});
